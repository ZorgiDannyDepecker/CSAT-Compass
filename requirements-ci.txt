# =============================================================================
# CSAT-Compass — CI-afhankelijkheden (GitHub Actions / Ubuntu)
# Bevat alleen wat nodig is om de testsuite te draaien.
# Zware pakketten (weasyprint, streamlit, plotly) worden weggelaten —
# die vereisen system-level libraries op Ubuntu (Cairo, pango, etc.)
# die niet beschikbaar zijn in de standaard GitHub Actions runner.
# matplotlib is wél beschikbaar op Ubuntu via pip zonder extra system-deps.
# =============================================================================

# --- Data laden (SQL + CSV) ---
pandas>=2.3.0
openpyxl>=3.1.0
sqlalchemy>=2.0.0
pyodbc>=5.0.0         # vereist: sudo apt-get install -y unixodbc-dev

# --- Visualisatie (matplotlib — geen system-deps op Ubuntu) ---
matplotlib>=3.7.0

# --- Utilities ---
python-dotenv>=1.0.0
loguru>=0.7.0
jinja2>=3.1.0           # rapport-templates (ReportExporter)

# --- Type stubs (mypy in CI) ---
pandas-stubs>=3.0.0

# --- Testing ---
pytest>=9.0.3
pytest-cov>=4.0
pytest-randomly>=3.15   # vereist door addopts: -p randomly

